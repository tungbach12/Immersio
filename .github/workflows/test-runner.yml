name: Test Self-Hosted Runner
on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - run: echo "✅ Runner hoạt động! Host: $(hostname), Date: $(date)"
      - run: docker --version
      - run: whoami
      - run: pwd
